class Historia {
  final int id;
  final String nombre;
  final String url;
  final String descripcion;
  final bool favorito;

  Historia(this.id, this.nombre, this.url, this.descripcion, this.favorito);
}